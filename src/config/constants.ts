export const API_BASE_URL = 'https://api.example.com'; // Replace with your actual API base URL
export const TOKEN_STORAGE_KEY = 'auth_token'; // Key for storing the JWT token in local storage
export const DEFAULT_PAGE_SIZE = 10; // Default number of items per page for pagination
export const MAX_UPLOAD_SIZE = 5 * 1024 * 1024; // Maximum upload size in bytes (5 MB)